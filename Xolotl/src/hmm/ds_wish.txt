
== Xolotl Data Structure Property Wishlist ==

We want disk IO that deals efficently with unused data, possibly
through mmap, so ideally the representation in memory should be
the same as the representation on disk.  We can use intermediate
data structures for data actually in use in many cases though.


=== Bloomfilter ===

See Couckoo filter notes


=== Ratchet Records ===

Is there a table optimized for a key of the form (n,i) where
n s random but i is sequential?  We want (n,i,j) of course.


DHRatchet
  

Parallel 

