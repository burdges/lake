
/*

pub struct ReplayFilter {
    capacity: usize,
    size: usize,
    ...
}

impl ReplayFilter {
    // Create a memory only ReplayFilter for testing
    pub fn new(capasity: usize) -> ReplayFilter {
    }

    // Initalize ReplayFilter synced to disk
    pub fn initialize<P: AsRef<Path>>(path: P, capasity: usize) -> io::Result<File> {
    }

    // Initalize ReplayFilter synced to disk
    pub fn new(file: File) -> io:Result<ReplayFilter> {
    }

    pub fn exists(&self, &[u8]) -> Bool {
    }

    pub fn insert(&self, &[u8]) -> Bool {
    }

    // pub fn delete(&self, &[u8]) -> Bool {
    // }

    pub fn size(&self) -> f32 {
        self.size
    }

    pub fn capasity(&self) -> usize {
        self.capasity
    }

    pub fn load(&self) -> f32 {
        self.size()/self.capasity();
    }

}

*/


